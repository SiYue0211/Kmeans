##kmeans(python实现)

目录说明：
- data: 存放数据集
- result: 对数据集进行聚类的结果
- kmeans.py: 对K均值聚类的实现
- run.py: main方法

使用工具：
- python
- numpy
- matplotlib
- pandas

##kmeans 原理
1. 创建k个点作为k个簇的起始质心（通常随机选择）
- 分别计算所有元素到这k个起始质心的相异度（距离），将这些元素分别划分到相异度最低的簇
- 根据聚类的结果，重新计算k个簇各自的中心，计算方法是取簇中所有元素各自维度的算数平均值
- 将所有元素按照新的中心进行聚类
- 重复上述步骤，直到聚类中心收敛
